import HC_UFO
define j = g u c d s b u~ c~ d~ s~ b~
generate g g > X0

output standalone $MADGRAPH_DIR/gg_X0

generate g g > X0 j

output standalone $MADGRAPH_DIR/gg_X0J

generate g g > X0 j j

output standalone $MADGRAPH_DIR/gg_X0JJ


quit
